<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <!--Dialog样式   没有四周边框-->
    <style name="ShareDialog"  parent="android:Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:background">@color/colorTransparent</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:backgroundDimAmount">0.7</item>
    </style>
<!--
        <item name="android:backgroundDimAmount">0.7</item>
        是改变透明度   1.0是全部遮挡
-->

    <!--自定义EditTextStyle
    colorControlNormal          默认状态状态设置底线颜色
    colorControlActivated       选择的底线颜色
    -->
    <style name="MyEditText" parent="Theme.AppCompat.Light">
        <item name="colorControlNormal">@color/colorEditTextControlActivated</item>
        <item name="colorControlActivated">@color/colorEditTextControlActivated</item>
    </style>


</resources>
